# Artifacts

Use this folder for temporary or large build/research artifacts that should not be tracked by Git by default.

For publishable artifacts, create a manifest in `reports/` or a release checklist.

