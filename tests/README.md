# Tests

Use this folder for unit, regression, and reproducibility tests.

Priority test targets:

- JSON parsing and fallback behavior in `llm_client.py`.
- Case model loading.
- Scoring schema parsing.
- Agent C score aggregation.
- Crash-resume state behavior.
- Config path resolution.

